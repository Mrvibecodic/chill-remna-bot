-- Снимок действующей подписки пользователя: локальной сущности подписки у
-- бота нет (истина в панели), а знать проданные условия нужно и бонусным
-- дням, и сверке лимитов.
ALTER TABLE users ADD COLUMN plan_snapshot TEXT NOT NULL DEFAULT '';
