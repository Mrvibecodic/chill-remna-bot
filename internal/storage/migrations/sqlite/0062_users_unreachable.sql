-- Когда Telegram последний раз ответил «чат недоступен» (человек заблокировал
-- бота или удалил аккаунт). Отдельно от blocked: тот флаг означает «админ
-- забанил» и закрывает доступ ко всему — ставить его платящему клиенту за то,
-- что он замьютил бота, нельзя. Здесь только «не тратить на него обращения»:
-- рассылка такого пропускает, а любое входящее сообщение метку снимает.
ALTER TABLE users ADD COLUMN unreachable_at TEXT NOT NULL DEFAULT '';
