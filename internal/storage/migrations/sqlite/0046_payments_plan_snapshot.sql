-- Снимок условий сделки (см. model.PlanSnapshot). Отдельными миграциями на
-- таблицу: раннер применяет по одному оператору на файл.
-- Колонка с DEFAULT '' — предыдущий образ бота её не видит и продолжает
-- писать свои строки, поэтому откат остаётся безопасным.
ALTER TABLE payments ADD COLUMN plan_snapshot TEXT NOT NULL DEFAULT '';
