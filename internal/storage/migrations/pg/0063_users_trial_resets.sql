-- Сколько раз боту разрешалось пройти пробный период заново: триал остался
-- неиспользованным (трафик ниже порога), и отметка «триал использован» была
-- снята. Счётчик держит потолок повторов из настроек триала — без него
-- человек, который никогда не покупает, крутил бы пробный период вечно.
ALTER TABLE users ADD COLUMN trial_resets INTEGER NOT NULL DEFAULT 0;
