-- Условия последнего продления: по ним видно, что тариф изменился и человека
-- надо предупредить. Само списание всегда идёт по действующему тарифу.
ALTER TABLE autopay ADD COLUMN plan_snapshot TEXT NOT NULL DEFAULT '';
