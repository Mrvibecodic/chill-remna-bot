-- Разовый подарочный трафик: сколько накинуто сверх потолка, каким был
-- счётчик расхода в момент выдачи и какой потолок бот записал. Хранится
-- снимком-строкой, как условия тарифа: полей три, и все они нужны только
-- вместе. Падение счётчика ниже записанного = панель обнулила период, и
-- подарок пора забирать.
ALTER TABLE users ADD COLUMN IF NOT EXISTS traffic_bonus TEXT NOT NULL DEFAULT '';
