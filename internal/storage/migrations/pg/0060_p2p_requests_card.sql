-- Реквизиты, выданные по этой заявке. Нужны, чтобы повторное нажатие
-- «Перевод на карту» вернуло ТУ ЖЕ карту, а не следующую по ротации:
-- человек мог уже перевести деньги на выданную.
ALTER TABLE p2p_requests ADD COLUMN card TEXT NOT NULL DEFAULT '';
