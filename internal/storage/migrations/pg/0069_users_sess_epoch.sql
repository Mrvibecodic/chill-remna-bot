-- Поколение пропусков ОДНОГО аккаунта. Общее поколение (в настройках)
-- разлогинивает всех сразу и для смены пароля не годится: сменивший пароль
-- обязан выбить чужие сессии, не трогая остальных.
ALTER TABLE users ADD COLUMN IF NOT EXISTS sess_epoch INTEGER NOT NULL DEFAULT 0;
